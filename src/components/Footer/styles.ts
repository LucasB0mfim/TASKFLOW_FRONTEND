import styled from "styled-components";

export const CopyRight = styled.p`
  position: fixed;
  bottom: 1%;
  left: 50%;

  color: #b6c2cf;
  font-size: 0.8vw;

  @media(max-width: 500px) {
    font-size: 0.5vh;
  }
`
